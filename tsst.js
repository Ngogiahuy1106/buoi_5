// 1//
number = Math.floor(Math.random()*16)+4
Number = []
function findEven(number) {
    for(i = 1 ; i < number; i++)
    {
        num = i/2 ;
        if(num.isInterger = true)
        {
            return num
        }
    }
    Number.push('num');
    console.log(Number)
}
2//
let button = document.getElementById('cau2')
console.log(button)
    button.addEventListener(('mouseover'),() => {
        button.setAttribute("style", "background=color: blue;")
    })
    button.addEventListener('mouseout', () => {
        button.setAttribute("style", "background=color: red;")
    })


3// 
function oneTriangle() {
    let collums = 4 
    triangle = ""
        for (let i = 1; i< collums; i++)
        {
            triangle = triangle + "1"+ '/n'
            return
            triangle 
        }
            document.getElementById("cau3").innerHTML = triangle;
}
oneTriangle()

 
